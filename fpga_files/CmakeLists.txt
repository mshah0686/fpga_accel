cmake_minimum_required(VERSION 3.16)
project(fpga_accel_sim CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

if(APPLE)
    list(APPEND CMAKE_PREFIX_PATH "/opt/homebrew")
    # Tell the linker exactly where to find Homebrew's compiled library files (.a / .dylib)
    link_directories("/opt/homebrew/lib")
endif()

find_package(verilator REQUIRED)

include_directories("${CMAKE_CURRENT_SOURCE_DIR}/rtl")

# add_subdirectory(tb/spi_cdc_handshake) # stale: references pre-split spi.v/spi_peripheral, needs rework
add_subdirectory(tb/spi_timer_control)
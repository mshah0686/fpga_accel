This test bench instantiates top version that has SPI controlling a timer interface
- TB will drive SPI signals
- No checking done
- Simply for waveform review of packet decoder and SPI sending 32 bit data and CDC 